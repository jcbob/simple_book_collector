# simple_book_collector
Uses Python imports, an interface between the program logic and a file-based memory database, as well as basic functions and structures to create a text-based, simple book collector
